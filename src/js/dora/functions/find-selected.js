/**
 * @module findSelected
 * @function findSelected that takes in an array of elements (dataList's children), loop through it and finds selected.
 * @property {title} title string
 */
// function findSelected(arr, selected) {
//   let dataList = Array.from(arr)
//   for (let i = 0; i < dataList.length; i++) {
//     if (dataList[i].classList.contains(selected)) {
//       return i
//     }
//   }
// }

export default findSelected